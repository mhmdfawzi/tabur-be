export enum UserRole {
  Admin = 'admin',
  Owner = 'owner',
  Manager = 'manager',
  Customer = 'customer',
}

export type UserRoleType = {
  Admin: string;
  Owner: string;
  Manager: string;
  Customer: string;
};
